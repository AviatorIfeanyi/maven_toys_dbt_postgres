maven_toys_dbt_postgres:
  outputs:

    dev:
      type: postgres
      threads: 2
      host: localhost
      port: 5432
      user: postgres
      pass: 'password'
      dbname: maven_toys
      schema: analytics


  target: dev
